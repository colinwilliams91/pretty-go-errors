{
  "extends": "../../tsconfig.base.json", // TODO: are all of these tsconfig.json problems due to their nested and duplicate nature?
  "compilerOptions": {
    "outDir": "dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
